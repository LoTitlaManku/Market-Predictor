

daily:
h=40
tt=30

hourly:
h=30
tt=10


cd ~/Market-Predictor

nohup uv run -m scripts.hyperparameter_tuning > training.log 2>&1 &

ps aux | grep hyperparameter_tuning

cat training.log

cat 'results/tuned_Profile A_1h.json'